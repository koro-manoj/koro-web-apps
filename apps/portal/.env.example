# Laravel API base URL (no trailing slash). Use :8080 for koro-go-services live mode.
NEXT_PUBLIC_API_URL=http://localhost:8080

# Set to "true" to use mock data when API is unavailable
NEXT_PUBLIC_USE_MOCK_API=true
